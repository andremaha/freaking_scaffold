<%= form_with(model: randomthought) do |form| %>
  <% if randomthought.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(randomthought.errors.count, "error") %> prohibited this randomthought from being saved:</h2>

      <ul>
        <% randomthought.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
